CREATE TABLE result (
    id INT AUTO_INCREMENT PRIMARY KEY,
    s_name VARCHAR(100) NOT NULL,
    s_rollno VARCHAR(20) NOT NULL,
    ORE INT DEFAULT 0,
    BD INT DEFAULT 0,
    BDA INT DEFAULT 0,
    IOT INT DEFAULT 0,
    DM INT DEFAULT 0,
    SC INT DEFAULT 0,
    MOG INT DEFAULT 0
);
